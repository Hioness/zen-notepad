# Workspace Conventions

## `temp/`

The `temp/` directory is a scratch workspace for exploratory outputs, prototypes, and non-deliverable presentations. Files here are ignored by git and are not part of the shipped application.

- Put throwaway HTML previews, design plans, and experimental snippets here.
- Do not commit files in `temp/`.
- Clean up `temp/` files when they are no longer needed.
